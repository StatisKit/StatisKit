
language: python

python: "2.7"

install:
    - "pip install -r REQUIREMENTS.txt"
    - "python setup.py install"
    - "pip install coveralls"

script:
    - "nosetests -v --with-coverage --cover-package=mngit"

after_success:
    coveralls
