branches:
  except:
  - doc
  - win
  
language: cpp

os:
  - linux
  - osx

sudo: required

services:
  - docker
  
env:
  # - CONDA_RECIPE=binutils
  # - CONDA_RECIPE=gmp
  # - CONDA_RECIPE=mpfr
  # - CONDA_RECIPE=mpc
  # - CONDA_RECIPE=isl
  # - CONDA_RECIPE=cloog
  # - CONDA_RECIPE=glibc
  # - CONDA_RECIPE=linux-headers
  # - CONDA_RECIPE=gcc
  #   TRAVIS_WAIT=50
  # - CONDA_RECIPE=libgcc
  # - CONDA_RECIPE=gitflow
  # - CONDA_RECIPE=vcs
  # - CONDA_RECIPE=python-scons
  #   CONDA_VERSION=2
  # - CONDA_RECIPE=python-scons
  #   CONDA_VERSION=3
  - CONDA_RECIPE=scons-tools
    CONDA_VERSION=2
  - CONDA_RECIPE=scons-tools
    CONDA_VERSION=3
  # - CONDA_RECIPE=python-parse
  #   CONDA_VERSION=2
  # - CONDA_RECIPE=python-parse
  #   CONDA_VERSION=3
  # - CONDA_RECIPE=libtoolchain
  #   CONDA_VERSION=2
  # - CONDA_RECIPE=libtoolchain
  #   CONDA_VERSION=3
  # - CONDA_RECIPE=libruntime
  # - CONDA_RECIPE=python-toolchain
  #   CONDA_VERSION=2
  # - CONDA_RECIPE=python-toolchain
  #   CONDA_VERSION=3
  # - CONDA_RECIPE=llvm
  # - CONDA_RECIPE=clang
  # - CONDA_RECIPE=python-clang
  #   CONDA_VERSION=2
  # - CONDA_RECIPE=python-clang
  #   CONDA_VERSION=3
  # - CONDA_RECIPE=libboost
  #   CONDA_VERSION=2
  #   TRAVIS_WAIT=30
  # - CONDA_RECIPE=libboost
  #   CONDA_VERSION=3
  #   TRAVIS_WAIT=30
  # - DOCKERFILE=ubuntu
  #   CONDA_VERSION=2
  # - DOCKERFILE=ubuntu
  #   CONDA_VERSION=3

matrix:
  exclude:
    - os: osx
      env: DOCKERFILE=ubuntu
           CONDA_VERSION=2
    - os: osx
      env: DOCKERFILE=ubuntu
           CONDA_VERSION=3
    - os: osx
      env: CONDA_RECIPE=glibc
    - os: osx
      env: CONDA_RECIPE=linux-headers
  # allow_failures:
  #   - os: osx
  #     env: CONDA_RECIPE=linux-headers
         
install:
  - git clone https://github.com/StatisKit/travis-ci.git
  - cd travis-ci
  - source install.sh

before_script:
  - source before_script.sh

script:
  - source script.sh

after_success:
  - source after_success.sh

after_failure:
  - source after_failure.sh

before_deploy:
  - source before_deploy.sh

deploy:
  skip_cleanup: true
  provider: script
  on:
      branch: master
  script: bash deploy_script.sh

after_deploy:
  - source after_deploy.sh

after_script:
  - source after_script.sh
