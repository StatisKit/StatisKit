branches:
  except:
  - doc
  - win
  
language: cpp

os:
  - linux
  - osx

sudo: required

services:
  - docker
  
env:
  # - RECIPE=glibc
  # - RECIPE=gmp
  # - RECIPE=mpfr
  # - RECIPE=mpc
  # - RECIPE=isl
  # - RECIPE=binutils
  # - RECIPE=gcc 
  # - RECIPE=libgcc
  - RECIPE=vcs
  - RECIPE=python-scons
  - RECIPE=python-parse
  - RECIPE=libdev
  - RECIPE=librun
  - RECIPE=python-dev
  - RECIPE=python-dbg
  - RECIPE=python-doc
  - RECIPE=python-pkg
  - RECIPE=llvm
  - RECIPE=clang
  - RECIPE=python-clang
  - RECIPE=libboost
  - UBUNTU=14.04
  - UBUNTU=16.04

matrix:
  exclude:
  - os: osx
    env: UBUNTU=14.04
  - os: osx
    env: UBUNTU=16.04
    
install:
  - git clone https://github.com/StatisKit/travis-ci.git
  - cd travis-ci
  - source install.sh

before_script:
  - source before_script.sh

script:
  - if [[ "$RECIPE" = "libboost" && "$TRAVIS_OS_NAME" = "linux" ]]; then
      export TRAVIS_WAIT=true;
    fi
  - source script.sh

after_success:
  - source after_success.sh

after_failure:
  - source after_failure.sh

before_deploy:
  - source before_deploy.sh

deploy:
  skip_cleanup: true
  provider: script
  on:
      branch: master
  script: bash deploy_script.sh

after_deploy:
  - source after_deploy.sh

after_script:
  - source after_script.sh
