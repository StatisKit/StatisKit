language: cpp

os:
  - linux
  - osx

sudo: required

env:
  # - RECIPE=gmp
  # - RECIPE=mpfr
  # - RECIPE=mpc
  # - RECIPE=isl
  # - RECIPE=binutils
  # - RECIPE=glibc
  # - RECIPE=gcc 
  # - RECIPE=libgcc
  - RECIPE=python-scons
  - RECIPE=libdev
  - RECIPE=python-dev
  - RECIPE=python-dbg
  - RECIPE=llvm
  - RECIPE=clang
  - RECIPE=python-clang
  - RECIPE=libboost

install:
  - git clone https://gist.github.com/319558c83d145620a6d4decdcadfbfb0.git travis-ci
  - cd travis-ci
  - source install-script.sh
  - cd ..

script:
  - cd conda
  - conda build $RECIPE -c statiskit -c conda-forge
  - cd ..

after_success:
  - echo "Success"

# before_deploy:
#   - cd travis-ci
#   - source before_deploy-script.sh
#   - cd ..

# deploy:
#   - cd conda;
#   - if [[ "$ANACONDA_DEPLOY" = "true" ]]; then
#       anaconda upload `conda build $RECIPE -c statiskit -c conda-forge --output` -u statiskit --force;
#     fi
#   - cd ..;


# after_deploy:
#   - cd travis-ci
#   - source after_deploy-script.sh
#   - cd ..