# -*-python-*-

Import("env")

conda_env = env.Clone(tools = ['conda'])

exclude = """
libboost
python-scons
""".split()

targets = conda_env.CondaBuild(exclude=exclude)
Alias("build", targets)
Alias("install", targets)

targets = conda_env.CondaInstall(exclude=exclude)
Alias("install", targets)