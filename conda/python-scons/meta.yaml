package:
  name: python-scons
  version: "2.5.1"

source:
  fn: scons-2.5.1.tar.gz
  url: https://sourceforge.net/projects/scons/files/scons/2.5.1/scons-2.5.1.tar.gz

build:
  number: 0

requirements:
  build:
    - python 2.7*
    - pyyaml
    - path.py
  run:
    - python 2.7*
    - pyyaml
    - path.py
test:
  files:
    - SConstruct
  commands:
    - scons $SCONSFLAGS     [not win]
    - scons                 [not win]

about:
  home: http://www.scons.org/
  license: MIT
  summary: 'Open Source next-generation build tool.'
